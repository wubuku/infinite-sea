// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.suiinfinitesea.restful.resource;

import java.util.*;
import java.util.stream.*;
import javax.servlet.http.*;
import javax.validation.constraints.*;
import org.springframework.http.MediaType;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import org.springframework.transaction.annotation.Transactional;
import org.dddml.support.criterion.*;
import org.dddml.suiinfinitesea.domain.*;
import java.math.BigInteger;
import java.util.Date;
import org.dddml.suiinfinitesea.specialization.*;
import org.dddml.suiinfinitesea.domain.itemproduction.*;
import static org.dddml.suiinfinitesea.domain.meta.M.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.dddml.support.criterion.TypeConverter;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@RequestMapping(path = "ItemProductions", produces = MediaType.APPLICATION_JSON_VALUE)
@RestController
public class ItemProductionResource {
    private Logger logger = LoggerFactory.getLogger(this.getClass());


    @Autowired
    private ItemProductionApplicationService itemProductionApplicationService;


    /**
     * Retrieve.
     * Retrieve ItemProductions
     */
    @GetMapping
    @Transactional(readOnly = true)
    public ItemProductionStateDto[] getAll( HttpServletRequest request,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "firstResult", defaultValue = "0") Integer firstResult,
                    @RequestParam(value = "maxResults", defaultValue = "2147483647") Integer maxResults,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
        if (firstResult < 0) { firstResult = 0; }
        if (maxResults == null || maxResults < 1) { maxResults = Integer.MAX_VALUE; }

            Iterable<ItemProductionState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> ItemProductionResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (ItemProductionMetadata.aliasMap.containsKey(n) ? ItemProductionMetadata.aliasMap.get(n) : n));
            states = itemProductionApplicationService.get(
                c,
                ItemProductionResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);

            ItemProductionStateDto.DtoConverter dtoConverter = new ItemProductionStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toItemProductionStateDtoArray(states);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve in pages.
     * Retrieve ItemProductions in pages.
     */
    @GetMapping("_page")
    @Transactional(readOnly = true)
    public Page<ItemProductionStateDto> getPage( HttpServletRequest request,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "page", defaultValue = "0") Integer page,
                    @RequestParam(value = "size", defaultValue = "20") Integer size,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
            Integer firstResult = (page == null ? 0 : page) * (size == null ? 20 : size);
            Integer maxResults = (size == null ? 20 : size);
            Iterable<ItemProductionState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> ItemProductionResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (ItemProductionMetadata.aliasMap.containsKey(n) ? ItemProductionMetadata.aliasMap.get(n) : n));
            states = itemProductionApplicationService.get(
                c,
                ItemProductionResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);
            long count = itemProductionApplicationService.getCount(c);

            ItemProductionStateDto.DtoConverter dtoConverter = new ItemProductionStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            Page.PageImpl<ItemProductionStateDto> statePage =  new Page.PageImpl<>(dtoConverter.toItemProductionStateDtoList(states), count);
            statePage.setSize(size);
            statePage.setNumber(page);
            return statePage;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve.
     * Retrieves ItemProduction with the specified ID.
     */
    @GetMapping("{itemProductionId}")
    @Transactional(readOnly = true)
    public ItemProductionStateDto get(@PathVariable("itemProductionId") String itemProductionId, @RequestParam(value = "fields", required = false) String fields) {
        try {
            SkillTypeItemIdPair idObj = ItemProductionResourceUtils.parseIdString(itemProductionId);
            ItemProductionState state = itemProductionApplicationService.get(idObj);
            if (state == null) { return null; }

            ItemProductionStateDto.DtoConverter dtoConverter = new ItemProductionStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toItemProductionStateDto(state);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_count")
    @Transactional(readOnly = true)
    public long getCount( HttpServletRequest request,
                         @RequestParam(value = "filter", required = false) String filter) {
        try {
            long count = 0;
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap());
            }
            Criterion c = CriterionDto.toSubclass(criterion,
                getCriterionTypeConverter(), 
                getPropertyTypeResolver(), 
                n -> (ItemProductionMetadata.aliasMap.containsKey(n) ? ItemProductionMetadata.aliasMap.get(n) : n));
            count = itemProductionApplicationService.getCount(c);
            return count;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    @PutMapping("{itemProductionId}/_commands/Create")
    public void create(@PathVariable("itemProductionId") String itemProductionId, @RequestBody ItemProductionCommands.Create content) {
        try {

            ItemProductionCommands.Create cmd = content;//.toCreate();
            SkillTypeItemIdPair idObj = ItemProductionResourceUtils.parseIdString(itemProductionId);
            if (cmd.getItemProductionId() == null) {
                cmd.setItemProductionId(idObj);
            } else if (!cmd.getItemProductionId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", itemProductionId, cmd.getItemProductionId());
            }
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            itemProductionApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    @PutMapping("{itemProductionId}/_commands/Update")
    public void update(@PathVariable("itemProductionId") String itemProductionId, @RequestBody ItemProductionCommands.Update content) {
        try {

            ItemProductionCommands.Update cmd = content;//.toUpdate();
            SkillTypeItemIdPair idObj = ItemProductionResourceUtils.parseIdString(itemProductionId);
            if (cmd.getItemProductionId() == null) {
                cmd.setItemProductionId(idObj);
            } else if (!cmd.getItemProductionId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", itemProductionId, cmd.getItemProductionId());
            }
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            itemProductionApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_metadata/filteringFields")
    public List<PropertyMetadataDto> getMetadataFilteringFields() {
        try {

            List<PropertyMetadataDto> filtering = new ArrayList<>();
            ItemProductionMetadata.propertyTypeMap.forEach((key, value) -> {
                filtering.add(new PropertyMetadataDto(key, value, true));
            });
            return filtering;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("{itemProductionId}/_events/{version}")
    @Transactional(readOnly = true)
    public ItemProductionEvent getEvent(@PathVariable("itemProductionId") String itemProductionId, @PathVariable("version") long version) {
        try {

            SkillTypeItemIdPair idObj = ItemProductionResourceUtils.parseIdString(itemProductionId);
            //ItemProductionStateEventDtoConverter dtoConverter = getItemProductionStateEventDtoConverter();
            return itemProductionApplicationService.getEvent(idObj, version);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("{itemProductionId}/_historyStates/{version}")
    @Transactional(readOnly = true)
    public ItemProductionStateDto getHistoryState(@PathVariable("itemProductionId") String itemProductionId, @PathVariable("version") long version, @RequestParam(value = "fields", required = false) String fields) {
        try {

            SkillTypeItemIdPair idObj = ItemProductionResourceUtils.parseIdString(itemProductionId);
            ItemProductionStateDto.DtoConverter dtoConverter = new ItemProductionStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toItemProductionStateDto(itemProductionApplicationService.getHistoryState(idObj, version));

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }



    //protected  ItemProductionStateEventDtoConverter getItemProductionStateEventDtoConverter() {
    //    return new ItemProductionStateEventDtoConverter();
    //}

    protected TypeConverter getCriterionTypeConverter() {
        return new DefaultTypeConverter();
    }

    protected PropertyTypeResolver getPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return ItemProductionResourceUtils.getFilterPropertyType(propertyName);
            }
        };
    }

    // ////////////////////////////////
 
    public static class ItemProductionResourceUtils {

        public static SkillTypeItemIdPair parseIdString(String idString) {
            TextFormatter<SkillTypeItemIdPair> formatter = ItemProductionMetadata.URL_ID_TEXT_FORMATTER;
            return formatter.parse(idString);
        }

        public static void setNullIdOrThrowOnInconsistentIds(String itemProductionId, ItemProductionCommand value) {
            SkillTypeItemIdPair idObj = parseIdString(itemProductionId);
            if (value.getItemProductionId() == null) {
                value.setItemProductionId(idObj);
            } else if (!value.getItemProductionId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", itemProductionId, value.getItemProductionId());
            }
        }
    
        public static List<String> getQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, ItemProductionMetadata.aliasMap);
        }

        public static List<String> getQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, ItemProductionMetadata.aliasMap);
        }

        public static String getFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (ItemProductionMetadata.aliasMap.containsKey(fieldName)) {
                return ItemProductionMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getFilterPropertyType(String propertyName) {
            if (ItemProductionMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = ItemProductionMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static ItemProductionStateDto[] toItemProductionStateDtoArray(Iterable<SkillTypeItemIdPair> ids) {
            List<ItemProductionStateDto> states = new ArrayList<>();
            ids.forEach(i -> {
                ItemProductionStateDto dto = new ItemProductionStateDto();
                dto.setItemProductionId(i);
                states.add(dto);
            });
            return states.toArray(new ItemProductionStateDto[0]);
        }

    }

}

