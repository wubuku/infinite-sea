// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.suiinfinitesea.restful.resource;

import java.util.*;
import java.util.stream.*;
import javax.servlet.http.*;
import javax.validation.constraints.*;
import org.springframework.http.MediaType;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import org.springframework.transaction.annotation.Transactional;
import org.dddml.support.criterion.*;
import org.dddml.suiinfinitesea.domain.*;
import java.math.BigInteger;
import java.util.Date;
import org.dddml.suiinfinitesea.specialization.*;
import org.dddml.suiinfinitesea.domain.itemcreation.*;
import static org.dddml.suiinfinitesea.domain.meta.M.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.dddml.support.criterion.TypeConverter;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@RequestMapping(path = "ItemCreations", produces = MediaType.APPLICATION_JSON_VALUE)
@RestController
public class ItemCreationResource {
    private Logger logger = LoggerFactory.getLogger(this.getClass());


    @Autowired
    private ItemCreationApplicationService itemCreationApplicationService;


    /**
     * Retrieve.
     * Retrieve ItemCreations
     */
    @GetMapping
    @Transactional(readOnly = true)
    public ItemCreationStateDto[] getAll( HttpServletRequest request,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "firstResult", defaultValue = "0") Integer firstResult,
                    @RequestParam(value = "maxResults", defaultValue = "2147483647") Integer maxResults,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
        if (firstResult < 0) { firstResult = 0; }
        if (maxResults == null || maxResults < 1) { maxResults = Integer.MAX_VALUE; }

            Iterable<ItemCreationState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> ItemCreationResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (ItemCreationMetadata.aliasMap.containsKey(n) ? ItemCreationMetadata.aliasMap.get(n) : n));
            states = itemCreationApplicationService.get(
                c,
                ItemCreationResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);

            ItemCreationStateDto.DtoConverter dtoConverter = new ItemCreationStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toItemCreationStateDtoArray(states);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve in pages.
     * Retrieve ItemCreations in pages.
     */
    @GetMapping("_page")
    @Transactional(readOnly = true)
    public Page<ItemCreationStateDto> getPage( HttpServletRequest request,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "page", defaultValue = "0") Integer page,
                    @RequestParam(value = "size", defaultValue = "20") Integer size,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
            Integer firstResult = (page == null ? 0 : page) * (size == null ? 20 : size);
            Integer maxResults = (size == null ? 20 : size);
            Iterable<ItemCreationState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> ItemCreationResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (ItemCreationMetadata.aliasMap.containsKey(n) ? ItemCreationMetadata.aliasMap.get(n) : n));
            states = itemCreationApplicationService.get(
                c,
                ItemCreationResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);
            long count = itemCreationApplicationService.getCount(c);

            ItemCreationStateDto.DtoConverter dtoConverter = new ItemCreationStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            Page.PageImpl<ItemCreationStateDto> statePage =  new Page.PageImpl<>(dtoConverter.toItemCreationStateDtoList(states), count);
            statePage.setSize(size);
            statePage.setNumber(page);
            return statePage;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve.
     * Retrieves ItemCreation with the specified ID.
     */
    @GetMapping("{itemCreationId}")
    @Transactional(readOnly = true)
    public ItemCreationStateDto get(@PathVariable("itemCreationId") String itemCreationId, @RequestParam(value = "fields", required = false) String fields) {
        try {
            SkillTypeItemIdPair idObj = ItemCreationResourceUtils.parseIdString(itemCreationId);
            ItemCreationState state = itemCreationApplicationService.get(idObj);
            if (state == null) { return null; }

            ItemCreationStateDto.DtoConverter dtoConverter = new ItemCreationStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toItemCreationStateDto(state);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_count")
    @Transactional(readOnly = true)
    public long getCount( HttpServletRequest request,
                         @RequestParam(value = "filter", required = false) String filter) {
        try {
            long count = 0;
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap());
            }
            Criterion c = CriterionDto.toSubclass(criterion,
                getCriterionTypeConverter(), 
                getPropertyTypeResolver(), 
                n -> (ItemCreationMetadata.aliasMap.containsKey(n) ? ItemCreationMetadata.aliasMap.get(n) : n));
            count = itemCreationApplicationService.getCount(c);
            return count;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    @PutMapping("{itemCreationId}/_commands/Create")
    public void create(@PathVariable("itemCreationId") String itemCreationId, @RequestBody ItemCreationCommands.Create content) {
        try {

            ItemCreationCommands.Create cmd = content;//.toCreate();
            SkillTypeItemIdPair idObj = ItemCreationResourceUtils.parseIdString(itemCreationId);
            if (cmd.getItemCreationId() == null) {
                cmd.setItemCreationId(idObj);
            } else if (!cmd.getItemCreationId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", itemCreationId, cmd.getItemCreationId());
            }
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            itemCreationApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    @PutMapping("{itemCreationId}/_commands/Update")
    public void update(@PathVariable("itemCreationId") String itemCreationId, @RequestBody ItemCreationCommands.Update content) {
        try {

            ItemCreationCommands.Update cmd = content;//.toUpdate();
            SkillTypeItemIdPair idObj = ItemCreationResourceUtils.parseIdString(itemCreationId);
            if (cmd.getItemCreationId() == null) {
                cmd.setItemCreationId(idObj);
            } else if (!cmd.getItemCreationId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", itemCreationId, cmd.getItemCreationId());
            }
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            itemCreationApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_metadata/filteringFields")
    public List<PropertyMetadataDto> getMetadataFilteringFields() {
        try {

            List<PropertyMetadataDto> filtering = new ArrayList<>();
            ItemCreationMetadata.propertyTypeMap.forEach((key, value) -> {
                filtering.add(new PropertyMetadataDto(key, value, true));
            });
            return filtering;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("{itemCreationId}/_events/{version}")
    @Transactional(readOnly = true)
    public ItemCreationEvent getEvent(@PathVariable("itemCreationId") String itemCreationId, @PathVariable("version") long version) {
        try {

            SkillTypeItemIdPair idObj = ItemCreationResourceUtils.parseIdString(itemCreationId);
            //ItemCreationStateEventDtoConverter dtoConverter = getItemCreationStateEventDtoConverter();
            return itemCreationApplicationService.getEvent(idObj, version);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("{itemCreationId}/_historyStates/{version}")
    @Transactional(readOnly = true)
    public ItemCreationStateDto getHistoryState(@PathVariable("itemCreationId") String itemCreationId, @PathVariable("version") long version, @RequestParam(value = "fields", required = false) String fields) {
        try {

            SkillTypeItemIdPair idObj = ItemCreationResourceUtils.parseIdString(itemCreationId);
            ItemCreationStateDto.DtoConverter dtoConverter = new ItemCreationStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toItemCreationStateDto(itemCreationApplicationService.getHistoryState(idObj, version));

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }



    //protected  ItemCreationStateEventDtoConverter getItemCreationStateEventDtoConverter() {
    //    return new ItemCreationStateEventDtoConverter();
    //}

    protected TypeConverter getCriterionTypeConverter() {
        return new DefaultTypeConverter();
    }

    protected PropertyTypeResolver getPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return ItemCreationResourceUtils.getFilterPropertyType(propertyName);
            }
        };
    }

    // ////////////////////////////////
 
    public static class ItemCreationResourceUtils {

        public static SkillTypeItemIdPair parseIdString(String idString) {
            TextFormatter<SkillTypeItemIdPair> formatter = ItemCreationMetadata.URL_ID_TEXT_FORMATTER;
            return formatter.parse(idString);
        }

        public static void setNullIdOrThrowOnInconsistentIds(String itemCreationId, ItemCreationCommand value) {
            SkillTypeItemIdPair idObj = parseIdString(itemCreationId);
            if (value.getItemCreationId() == null) {
                value.setItemCreationId(idObj);
            } else if (!value.getItemCreationId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", itemCreationId, value.getItemCreationId());
            }
        }
    
        public static List<String> getQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, ItemCreationMetadata.aliasMap);
        }

        public static List<String> getQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, ItemCreationMetadata.aliasMap);
        }

        public static String getFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (ItemCreationMetadata.aliasMap.containsKey(fieldName)) {
                return ItemCreationMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getFilterPropertyType(String propertyName) {
            if (ItemCreationMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = ItemCreationMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static ItemCreationStateDto[] toItemCreationStateDtoArray(Iterable<SkillTypeItemIdPair> ids) {
            List<ItemCreationStateDto> states = new ArrayList<>();
            ids.forEach(i -> {
                ItemCreationStateDto dto = new ItemCreationStateDto();
                dto.setItemCreationId(i);
                states.add(dto);
            });
            return states.toArray(new ItemCreationStateDto[0]);
        }

    }

}

