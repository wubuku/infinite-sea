// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.suiinfinitesea.restful.resource;

import java.util.*;
import java.util.stream.*;
import javax.servlet.http.*;
import javax.validation.constraints.*;
import org.springframework.http.MediaType;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import org.springframework.transaction.annotation.Transactional;
import org.dddml.support.criterion.*;
import org.dddml.suiinfinitesea.domain.*;
import java.math.BigInteger;
import java.util.Date;
import org.dddml.suiinfinitesea.specialization.*;
import org.dddml.suiinfinitesea.domain.skillprocess.*;
import static org.dddml.suiinfinitesea.domain.meta.M.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.dddml.support.criterion.TypeConverter;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@RequestMapping(path = "SkillProcesses", produces = MediaType.APPLICATION_JSON_VALUE)
@RestController
public class SkillProcessResource {
    private Logger logger = LoggerFactory.getLogger(this.getClass());


    @Autowired
    private SkillProcessApplicationService skillProcessApplicationService;


    /**
     * Retrieve.
     * Retrieve SkillProcesses
     */
    @GetMapping
    @Transactional(readOnly = true)
    public SkillProcessStateDto[] getAll( HttpServletRequest request,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "firstResult", defaultValue = "0") Integer firstResult,
                    @RequestParam(value = "maxResults", defaultValue = "2147483647") Integer maxResults,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
        if (firstResult < 0) { firstResult = 0; }
        if (maxResults == null || maxResults < 1) { maxResults = Integer.MAX_VALUE; }

            Iterable<SkillProcessState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> SkillProcessResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (SkillProcessMetadata.aliasMap.containsKey(n) ? SkillProcessMetadata.aliasMap.get(n) : n));
            states = skillProcessApplicationService.get(
                c,
                SkillProcessResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);

            SkillProcessStateDto.DtoConverter dtoConverter = new SkillProcessStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toSkillProcessStateDtoArray(states);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve in pages.
     * Retrieve SkillProcesses in pages.
     */
    @GetMapping("_page")
    @Transactional(readOnly = true)
    public Page<SkillProcessStateDto> getPage( HttpServletRequest request,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "page", defaultValue = "0") Integer page,
                    @RequestParam(value = "size", defaultValue = "20") Integer size,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
            Integer firstResult = (page == null ? 0 : page) * (size == null ? 20 : size);
            Integer maxResults = (size == null ? 20 : size);
            Iterable<SkillProcessState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> SkillProcessResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (SkillProcessMetadata.aliasMap.containsKey(n) ? SkillProcessMetadata.aliasMap.get(n) : n));
            states = skillProcessApplicationService.get(
                c,
                SkillProcessResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);
            long count = skillProcessApplicationService.getCount(c);

            SkillProcessStateDto.DtoConverter dtoConverter = new SkillProcessStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            Page.PageImpl<SkillProcessStateDto> statePage =  new Page.PageImpl<>(dtoConverter.toSkillProcessStateDtoList(states), count);
            statePage.setSize(size);
            statePage.setNumber(page);
            return statePage;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve.
     * Retrieves SkillProcess with the specified ID.
     */
    @GetMapping("{skillProcessId}")
    @Transactional(readOnly = true)
    public SkillProcessStateDto get(@PathVariable("skillProcessId") String skillProcessId, @RequestParam(value = "fields", required = false) String fields) {
        try {
            SkillTypePlayerIdPair idObj = SkillProcessResourceUtils.parseIdString(skillProcessId);
            SkillProcessState state = skillProcessApplicationService.get(idObj);
            if (state == null) { return null; }

            SkillProcessStateDto.DtoConverter dtoConverter = new SkillProcessStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toSkillProcessStateDto(state);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_count")
    @Transactional(readOnly = true)
    public long getCount( HttpServletRequest request,
                         @RequestParam(value = "filter", required = false) String filter) {
        try {
            long count = 0;
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap());
            }
            Criterion c = CriterionDto.toSubclass(criterion,
                getCriterionTypeConverter(), 
                getPropertyTypeResolver(), 
                n -> (SkillProcessMetadata.aliasMap.containsKey(n) ? SkillProcessMetadata.aliasMap.get(n) : n));
            count = skillProcessApplicationService.getCount(c);
            return count;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    @PutMapping("{skillProcessId}/_commands/Create")
    public void create(@PathVariable("skillProcessId") String skillProcessId, @RequestBody SkillProcessCommands.Create content) {
        try {

            SkillProcessCommands.Create cmd = content;//.toCreate();
            SkillTypePlayerIdPair idObj = SkillProcessResourceUtils.parseIdString(skillProcessId);
            if (cmd.getSkillProcessId() == null) {
                cmd.setSkillProcessId(idObj);
            } else if (!cmd.getSkillProcessId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", skillProcessId, cmd.getSkillProcessId());
            }
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            skillProcessApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    @PutMapping("{skillProcessId}/_commands/CompleteProduction")
    public void completeProduction(@PathVariable("skillProcessId") String skillProcessId, @RequestBody SkillProcessCommands.CompleteProduction content) {
        try {

            SkillProcessCommands.CompleteProduction cmd = content;//.toCompleteProduction();
            SkillTypePlayerIdPair idObj = SkillProcessResourceUtils.parseIdString(skillProcessId);
            if (cmd.getSkillProcessId() == null) {
                cmd.setSkillProcessId(idObj);
            } else if (!cmd.getSkillProcessId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", skillProcessId, cmd.getSkillProcessId());
            }
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            skillProcessApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    @PutMapping("{skillProcessId}/_commands/CompleteMutexCreation")
    public void completeMutexCreation(@PathVariable("skillProcessId") String skillProcessId, @RequestBody SkillProcessCommands.CompleteMutexCreation content) {
        try {

            SkillProcessCommands.CompleteMutexCreation cmd = content;//.toCompleteMutexCreation();
            SkillTypePlayerIdPair idObj = SkillProcessResourceUtils.parseIdString(skillProcessId);
            if (cmd.getSkillProcessId() == null) {
                cmd.setSkillProcessId(idObj);
            } else if (!cmd.getSkillProcessId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", skillProcessId, cmd.getSkillProcessId());
            }
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            skillProcessApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_metadata/filteringFields")
    public List<PropertyMetadataDto> getMetadataFilteringFields() {
        try {

            List<PropertyMetadataDto> filtering = new ArrayList<>();
            SkillProcessMetadata.propertyTypeMap.forEach((key, value) -> {
                filtering.add(new PropertyMetadataDto(key, value, true));
            });
            return filtering;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("{skillProcessId}/_events/{version}")
    @Transactional(readOnly = true)
    public SkillProcessEvent getEvent(@PathVariable("skillProcessId") String skillProcessId, @PathVariable("version") long version) {
        try {

            SkillTypePlayerIdPair idObj = SkillProcessResourceUtils.parseIdString(skillProcessId);
            //SkillProcessStateEventDtoConverter dtoConverter = getSkillProcessStateEventDtoConverter();
            return skillProcessApplicationService.getEvent(idObj, version);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("{skillProcessId}/_historyStates/{version}")
    @Transactional(readOnly = true)
    public SkillProcessStateDto getHistoryState(@PathVariable("skillProcessId") String skillProcessId, @PathVariable("version") long version, @RequestParam(value = "fields", required = false) String fields) {
        try {

            SkillTypePlayerIdPair idObj = SkillProcessResourceUtils.parseIdString(skillProcessId);
            SkillProcessStateDto.DtoConverter dtoConverter = new SkillProcessStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toSkillProcessStateDto(skillProcessApplicationService.getHistoryState(idObj, version));

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }



    //protected  SkillProcessStateEventDtoConverter getSkillProcessStateEventDtoConverter() {
    //    return new SkillProcessStateEventDtoConverter();
    //}

    protected TypeConverter getCriterionTypeConverter() {
        return new DefaultTypeConverter();
    }

    protected PropertyTypeResolver getPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return SkillProcessResourceUtils.getFilterPropertyType(propertyName);
            }
        };
    }

    // ////////////////////////////////
 
    public static class SkillProcessResourceUtils {

        public static SkillTypePlayerIdPair parseIdString(String idString) {
            TextFormatter<SkillTypePlayerIdPair> formatter = SkillProcessMetadata.URL_ID_TEXT_FORMATTER;
            return formatter.parse(idString);
        }

        public static void setNullIdOrThrowOnInconsistentIds(String skillProcessId, SkillProcessCommand value) {
            SkillTypePlayerIdPair idObj = parseIdString(skillProcessId);
            if (value.getSkillProcessId() == null) {
                value.setSkillProcessId(idObj);
            } else if (!value.getSkillProcessId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", skillProcessId, value.getSkillProcessId());
            }
        }
    
        public static List<String> getQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, SkillProcessMetadata.aliasMap);
        }

        public static List<String> getQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, SkillProcessMetadata.aliasMap);
        }

        public static String getFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (SkillProcessMetadata.aliasMap.containsKey(fieldName)) {
                return SkillProcessMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getFilterPropertyType(String propertyName) {
            if (SkillProcessMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = SkillProcessMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static SkillProcessStateDto[] toSkillProcessStateDtoArray(Iterable<SkillTypePlayerIdPair> ids) {
            List<SkillProcessStateDto> states = new ArrayList<>();
            ids.forEach(i -> {
                SkillProcessStateDto dto = new SkillProcessStateDto();
                dto.setSkillProcessId(i);
                states.add(dto);
            });
            return states.toArray(new SkillProcessStateDto[0]);
        }

    }

}

