// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.suiinfinitesea.restful.resource;

import java.util.*;
import java.util.stream.*;
import javax.servlet.http.*;
import javax.validation.constraints.*;
import org.springframework.http.MediaType;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import org.springframework.transaction.annotation.Transactional;
import org.dddml.support.criterion.*;
import java.math.BigInteger;
import java.util.Date;
import org.dddml.suiinfinitesea.domain.*;
import org.dddml.suiinfinitesea.specialization.*;
import org.dddml.suiinfinitesea.domain.avatarchange.*;
import static org.dddml.suiinfinitesea.domain.meta.M.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.dddml.support.criterion.TypeConverter;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@RequestMapping(path = "AvatarChanges", produces = MediaType.APPLICATION_JSON_VALUE)
@RestController
public class AvatarChangeResource {
    private Logger logger = LoggerFactory.getLogger(this.getClass());


    @Autowired
    private AvatarChangeApplicationService avatarChangeApplicationService;


    /**
     * Retrieve.
     * Retrieve AvatarChanges
     */
    @GetMapping
    @Transactional(readOnly = true)
    public AvatarChangeStateDto[] getAll( HttpServletRequest request,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "firstResult", defaultValue = "0") Integer firstResult,
                    @RequestParam(value = "maxResults", defaultValue = "2147483647") Integer maxResults,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
        if (firstResult < 0) { firstResult = 0; }
        if (maxResults == null || maxResults < 1) { maxResults = Integer.MAX_VALUE; }

            Iterable<AvatarChangeState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> AvatarChangeResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (AvatarChangeMetadata.aliasMap.containsKey(n) ? AvatarChangeMetadata.aliasMap.get(n) : n));
            states = avatarChangeApplicationService.get(
                c,
                AvatarChangeResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);

            AvatarChangeStateDto.DtoConverter dtoConverter = new AvatarChangeStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toAvatarChangeStateDtoArray(states);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve in pages.
     * Retrieve AvatarChanges in pages.
     */
    @GetMapping("_page")
    @Transactional(readOnly = true)
    public Page<AvatarChangeStateDto> getPage( HttpServletRequest request,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "page", defaultValue = "0") Integer page,
                    @RequestParam(value = "size", defaultValue = "20") Integer size,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
            Integer firstResult = (page == null ? 0 : page) * (size == null ? 20 : size);
            Integer maxResults = (size == null ? 20 : size);
            Iterable<AvatarChangeState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> AvatarChangeResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (AvatarChangeMetadata.aliasMap.containsKey(n) ? AvatarChangeMetadata.aliasMap.get(n) : n));
            states = avatarChangeApplicationService.get(
                c,
                AvatarChangeResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);
            long count = avatarChangeApplicationService.getCount(c);

            AvatarChangeStateDto.DtoConverter dtoConverter = new AvatarChangeStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            Page.PageImpl<AvatarChangeStateDto> statePage =  new Page.PageImpl<>(dtoConverter.toAvatarChangeStateDtoList(states), count);
            statePage.setSize(size);
            statePage.setNumber(page);
            return statePage;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve.
     * Retrieves AvatarChange with the specified ID.
     */
    @GetMapping("{avatarId}")
    @Transactional(readOnly = true)
    public AvatarChangeStateDto get(@PathVariable("avatarId") String avatarId, @RequestParam(value = "fields", required = false) String fields) {
        try {
            String idObj = avatarId;
            AvatarChangeState state = avatarChangeApplicationService.get(idObj);
            if (state == null) { return null; }

            AvatarChangeStateDto.DtoConverter dtoConverter = new AvatarChangeStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toAvatarChangeStateDto(state);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_count")
    @Transactional(readOnly = true)
    public long getCount( HttpServletRequest request,
                         @RequestParam(value = "filter", required = false) String filter) {
        try {
            long count = 0;
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap());
            }
            Criterion c = CriterionDto.toSubclass(criterion,
                getCriterionTypeConverter(), 
                getPropertyTypeResolver(), 
                n -> (AvatarChangeMetadata.aliasMap.containsKey(n) ? AvatarChangeMetadata.aliasMap.get(n) : n));
            count = avatarChangeApplicationService.getCount(c);
            return count;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    @PutMapping("{avatarId}/_commands/Create")
    public void create(@PathVariable("avatarId") String avatarId, @RequestBody AvatarChangeCommands.Create content) {
        try {

            AvatarChangeCommands.Create cmd = content;//.toCreate();
            String idObj = avatarId;
            if (cmd.getAvatarId() == null) {
                cmd.setAvatarId(idObj);
            } else if (!cmd.getAvatarId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", avatarId, cmd.getAvatarId());
            }
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            avatarChangeApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    @PutMapping("{avatarId}/_commands/Update")
    public void update(@PathVariable("avatarId") String avatarId, @RequestBody AvatarChangeCommands.Update content) {
        try {

            AvatarChangeCommands.Update cmd = content;//.toUpdate();
            String idObj = avatarId;
            if (cmd.getAvatarId() == null) {
                cmd.setAvatarId(idObj);
            } else if (!cmd.getAvatarId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", avatarId, cmd.getAvatarId());
            }
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            avatarChangeApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    @PutMapping("{avatarId}/_commands/Delete")
    public void delete(@PathVariable("avatarId") String avatarId, @RequestBody AvatarChangeCommands.Delete content) {
        try {

            AvatarChangeCommands.Delete cmd = content;//.toDelete();
            String idObj = avatarId;
            if (cmd.getAvatarId() == null) {
                cmd.setAvatarId(idObj);
            } else if (!cmd.getAvatarId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", avatarId, cmd.getAvatarId());
            }
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            avatarChangeApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_metadata/filteringFields")
    public List<PropertyMetadataDto> getMetadataFilteringFields() {
        try {

            List<PropertyMetadataDto> filtering = new ArrayList<>();
            AvatarChangeMetadata.propertyTypeMap.forEach((key, value) -> {
                filtering.add(new PropertyMetadataDto(key, value, true));
            });
            return filtering;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("{avatarId}/_events/{version}")
    @Transactional(readOnly = true)
    public AvatarChangeEvent getEvent(@PathVariable("avatarId") String avatarId, @PathVariable("version") long version) {
        try {

            String idObj = avatarId;
            //AvatarChangeStateEventDtoConverter dtoConverter = getAvatarChangeStateEventDtoConverter();
            return avatarChangeApplicationService.getEvent(idObj, version);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("{avatarId}/_historyStates/{version}")
    @Transactional(readOnly = true)
    public AvatarChangeStateDto getHistoryState(@PathVariable("avatarId") String avatarId, @PathVariable("version") long version, @RequestParam(value = "fields", required = false) String fields) {
        try {

            String idObj = avatarId;
            AvatarChangeStateDto.DtoConverter dtoConverter = new AvatarChangeStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toAvatarChangeStateDto(avatarChangeApplicationService.getHistoryState(idObj, version));

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }



    //protected  AvatarChangeStateEventDtoConverter getAvatarChangeStateEventDtoConverter() {
    //    return new AvatarChangeStateEventDtoConverter();
    //}

    protected TypeConverter getCriterionTypeConverter() {
        return new DefaultTypeConverter();
    }

    protected PropertyTypeResolver getPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return AvatarChangeResourceUtils.getFilterPropertyType(propertyName);
            }
        };
    }

    // ////////////////////////////////
 
    public static class AvatarChangeResourceUtils {

        public static void setNullIdOrThrowOnInconsistentIds(String avatarId, org.dddml.suiinfinitesea.domain.avatarchange.AvatarChangeCommand value) {
            String idObj = avatarId;
            if (value.getAvatarId() == null) {
                value.setAvatarId(idObj);
            } else if (!value.getAvatarId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", avatarId, value.getAvatarId());
            }
        }
    
        public static List<String> getQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, AvatarChangeMetadata.aliasMap);
        }

        public static List<String> getQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, AvatarChangeMetadata.aliasMap);
        }

        public static String getFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (AvatarChangeMetadata.aliasMap.containsKey(fieldName)) {
                return AvatarChangeMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getFilterPropertyType(String propertyName) {
            if (AvatarChangeMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = AvatarChangeMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static AvatarChangeStateDto[] toAvatarChangeStateDtoArray(Iterable<String> ids) {
            List<AvatarChangeStateDto> states = new ArrayList<>();
            ids.forEach(i -> {
                AvatarChangeStateDto dto = new AvatarChangeStateDto();
                dto.setAvatarId(i);
                states.add(dto);
            });
            return states.toArray(new AvatarChangeStateDto[0]);
        }

    }

}

