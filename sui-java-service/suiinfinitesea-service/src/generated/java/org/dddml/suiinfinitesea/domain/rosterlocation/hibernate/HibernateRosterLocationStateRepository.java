// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.suiinfinitesea.domain.rosterlocation.hibernate;

import java.util.*;
import org.dddml.suiinfinitesea.domain.*;
import java.math.BigInteger;
import java.util.Date;
import org.hibernate.Session;
import org.hibernate.Criteria;
//import org.hibernate.criterion.Order;
import org.hibernate.criterion.Criterion;
import org.hibernate.criterion.Projections;
import org.hibernate.SessionFactory;
import org.dddml.suiinfinitesea.domain.rosterlocation.*;
import org.dddml.suiinfinitesea.specialization.*;
import org.dddml.suiinfinitesea.specialization.hibernate.*;
import org.springframework.transaction.annotation.Transactional;

public class HibernateRosterLocationStateRepository implements RosterLocationStateRepository {
    private SessionFactory sessionFactory;

    public SessionFactory getSessionFactory() { return this.sessionFactory; }

    public void setSessionFactory(SessionFactory sessionFactory) { this.sessionFactory = sessionFactory; }

    protected Session getCurrentSession() {
        return this.sessionFactory.getCurrentSession();
    }
    
    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("RosterObjectId", "Coordinates", "Status", "Version", "OffChainVersion", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt", "Active", "Deleted"));
    
    private ReadOnlyProxyGenerator readOnlyProxyGenerator;
    
    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public RosterLocationState get(String id, boolean nullAllowed) {
        RosterLocationState.SqlRosterLocationState state = (RosterLocationState.SqlRosterLocationState)getCurrentSession().get(AbstractRosterLocationState.SimpleRosterLocationState.class, id);
        if (!nullAllowed && state == null) {
            state = new AbstractRosterLocationState.SimpleRosterLocationState();
            state.setRosterObjectId(id);
        }
        if (getReadOnlyProxyGenerator() != null && state != null) {
            return (RosterLocationState) getReadOnlyProxyGenerator().createProxy(state, new Class[]{RosterLocationState.SqlRosterLocationState.class}, "getStateReadOnly", readOnlyPropertyPascalCaseNames);
        }
        return state;
    }

    public void save(RosterLocationState state) {
        RosterLocationState s = state;
        if (getReadOnlyProxyGenerator() != null) {
            s = (RosterLocationState) getReadOnlyProxyGenerator().getTarget(state);
        }
        if(s.getOffChainVersion() == null) {
            getCurrentSession().save(s);
        } else {
            getCurrentSession().update(s);
        }

        if (s instanceof Saveable)
        {
            Saveable saveable = (Saveable) s;
            saveable.save();
        }
        getCurrentSession().flush();
    }

    public void merge(RosterLocationState detached) {
        RosterLocationState persistent = getCurrentSession().get(AbstractRosterLocationState.SimpleRosterLocationState.class, detached.getRosterObjectId());
        if (persistent != null) {
            merge(persistent, detached);
            getCurrentSession().save(persistent);
        } else {
            getCurrentSession().save(detached);
        }
        getCurrentSession().flush();
    }

    private void merge(RosterLocationState persistent, RosterLocationState detached) {
        ((AbstractRosterLocationState) persistent).merge(detached);
    }

}

