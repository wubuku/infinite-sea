// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.suiinfinitesea.domain.whitelist.hibernate;

import java.util.Date;
import org.dddml.suiinfinitesea.domain.*;
import org.hibernate.*;
import org.hibernate.criterion.*;
import org.dddml.suiinfinitesea.domain.whitelist.*;
import org.dddml.suiinfinitesea.specialization.*;
import org.springframework.transaction.annotation.Transactional;

public class HibernateWhitelistEntryEventDao implements WhitelistEntryEventDao {
    private SessionFactory sessionFactory;

    public SessionFactory getSessionFactory() { return this.sessionFactory; }

    public void setSessionFactory(SessionFactory sessionFactory) { this.sessionFactory = sessionFactory; }

    protected Session getCurrentSession() {
        return this.sessionFactory.getCurrentSession();
    }

    @Override
    public void save(WhitelistEntryEvent e)
    {
        getCurrentSession().save(e);
        if (e instanceof Saveable)
        {
            Saveable saveable = (Saveable) e;
            saveable.save();
        }
    }


    @Transactional(readOnly = true)
    @Override
    public Iterable<WhitelistEntryEvent> findByWhitelistEventId(WhitelistEventId whitelistEventId)
    {
        Criteria criteria = getCurrentSession().createCriteria(AbstractWhitelistEntryEvent.class);
        Junction partIdCondition = Restrictions.conjunction()
            .add(Restrictions.eq("whitelistEntryEventId.whitelistId", whitelistEventId.getId()))
            .add(Restrictions.eq("whitelistEntryEventId.version", whitelistEventId.getVersion()))
            ;
        return criteria.add(partIdCondition).list();
    }

}

