// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.suiinfinitesea.domain.faucetrequested.hibernate;

import java.util.*;
import java.math.BigInteger;
import org.dddml.suiinfinitesea.domain.*;
import java.util.Date;
import org.hibernate.Session;
import org.hibernate.Criteria;
//import org.hibernate.criterion.Order;
import org.hibernate.criterion.Criterion;
import org.hibernate.criterion.Projections;
import org.hibernate.SessionFactory;
import org.dddml.suiinfinitesea.domain.faucetrequested.*;
import org.dddml.suiinfinitesea.specialization.*;
import org.dddml.suiinfinitesea.specialization.hibernate.*;
import org.springframework.transaction.annotation.Transactional;

public class HibernateFaucetRequestedStateRepository implements FaucetRequestedStateRepository {
    private SessionFactory sessionFactory;

    public SessionFactory getSessionFactory() { return this.sessionFactory; }

    public void setSessionFactory(SessionFactory sessionFactory) { this.sessionFactory = sessionFactory; }

    protected Session getCurrentSession() {
        return this.sessionFactory.getCurrentSession();
    }
    
    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("EventId", "RequesterAccount", "RequestedAmount", "Description", "SuiPackageId", "SuiTransactionModule", "SuiSender", "SuiType", "SuiTimestamp", "SuiTxDigest", "SuiEventSeq", "NextCursor", "Version", "OffChainVersion", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt", "Active", "Deleted"));
    
    private ReadOnlyProxyGenerator readOnlyProxyGenerator;
    
    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public FaucetRequestedState get(String id, boolean nullAllowed) {
        FaucetRequestedState.SqlFaucetRequestedState state = (FaucetRequestedState.SqlFaucetRequestedState)getCurrentSession().get(AbstractFaucetRequestedState.SimpleFaucetRequestedState.class, id);
        if (!nullAllowed && state == null) {
            state = new AbstractFaucetRequestedState.SimpleFaucetRequestedState();
            state.setEventId(id);
        }
        return state;
    }

    @Transactional
    public void save(FaucetRequestedState state) {
        FaucetRequestedState s = state;
        if(s.getOffChainVersion() == null) {
            getCurrentSession().save(s);
        } else {
            getCurrentSession().update(s);
        }

        if (s instanceof Saveable)
        {
            Saveable saveable = (Saveable) s;
            saveable.save();
        }
        getCurrentSession().flush();
    }

    public void merge(FaucetRequestedState detached) {
        FaucetRequestedState persistent = getCurrentSession().get(AbstractFaucetRequestedState.SimpleFaucetRequestedState.class, detached.getEventId());
        if (persistent != null) {
            merge(persistent, detached);
            getCurrentSession().save(persistent);
        } else {
            getCurrentSession().save(detached);
        }
        getCurrentSession().flush();
    }

    private void merge(FaucetRequestedState persistent, FaucetRequestedState detached) {
        ((AbstractFaucetRequestedState) persistent).merge(detached);
    }

}

