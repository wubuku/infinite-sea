// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.suiinfinitesea.domain.itemproduction.hibernate;

import java.util.*;
import org.dddml.suiinfinitesea.domain.*;
import java.math.BigInteger;
import java.util.Date;
import org.hibernate.Session;
import org.hibernate.Criteria;
//import org.hibernate.criterion.Order;
import org.hibernate.criterion.Criterion;
import org.hibernate.criterion.Projections;
import org.hibernate.SessionFactory;
import org.dddml.suiinfinitesea.domain.itemproduction.*;
import org.dddml.suiinfinitesea.specialization.*;
import org.dddml.suiinfinitesea.specialization.hibernate.*;
import org.springframework.transaction.annotation.Transactional;

public class HibernateItemProductionStateRepository implements ItemProductionStateRepository {
    private SessionFactory sessionFactory;

    public SessionFactory getSessionFactory() { return this.sessionFactory; }

    public void setSessionFactory(SessionFactory sessionFactory) { this.sessionFactory = sessionFactory; }

    protected Session getCurrentSession() {
        return this.sessionFactory.getCurrentSession();
    }
    
    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("ItemProductionId", "ProductionMaterials", "RequirementsLevel", "BaseQuantity", "BaseExperience", "BaseCreationTime", "EnergyCost", "SuccessRate", "Version", "OffChainVersion", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt", "Active", "Deleted"));
    
    private ReadOnlyProxyGenerator readOnlyProxyGenerator;
    
    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public ItemProductionState get(SkillTypeItemIdPair id, boolean nullAllowed) {
        ItemProductionState.SqlItemProductionState state = (ItemProductionState.SqlItemProductionState)getCurrentSession().get(AbstractItemProductionState.SimpleItemProductionState.class, id);
        if (!nullAllowed && state == null) {
            state = new AbstractItemProductionState.SimpleItemProductionState();
            state.setItemProductionId(id);
        }
        if (getReadOnlyProxyGenerator() != null && state != null) {
            return (ItemProductionState) getReadOnlyProxyGenerator().createProxy(state, new Class[]{ItemProductionState.SqlItemProductionState.class}, "getStateReadOnly", readOnlyPropertyPascalCaseNames);
        }
        return state;
    }

    public void save(ItemProductionState state) {
        ItemProductionState s = state;
        if (getReadOnlyProxyGenerator() != null) {
            s = (ItemProductionState) getReadOnlyProxyGenerator().getTarget(state);
        }
        if(s.getOffChainVersion() == null) {
            getCurrentSession().save(s);
        } else {
            getCurrentSession().update(s);
        }

        if (s instanceof Saveable)
        {
            Saveable saveable = (Saveable) s;
            saveable.save();
        }
        getCurrentSession().flush();
    }

    public void merge(ItemProductionState detached) {
        ItemProductionState persistent = getCurrentSession().get(AbstractItemProductionState.SimpleItemProductionState.class, detached.getItemProductionId());
        if (persistent != null) {
            merge(persistent, detached);
            getCurrentSession().save(persistent);
        } else {
            getCurrentSession().save(detached);
        }
        getCurrentSession().flush();
    }

    private void merge(ItemProductionState persistent, ItemProductionState detached) {
        ((AbstractItemProductionState) persistent).merge(detached);
    }

}

